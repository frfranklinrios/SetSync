# Screenshots da homepage SetSync

Imagens usadas na seção **Veja o SetSync em ação** (`partials/secao_screenshots.html`).

| Arquivo | Descrição | Dimensão atual |
|---------|-----------|----------------|
| `modo-tocar-mobile.png` | Modo Tocar no celular — cifra em tela cheia | **540 × 1170 px** |
| `setlist-desktop.png` | Painel de setlists no desktop | **1389 × 822 px** |

Formato PNG (ou JPEG com extensão `.png` — o navegador renderiza normalmente).
Fundo escuro coerente com o tema do produto; o layout aplica moldura de celular e sombra no desktop.
